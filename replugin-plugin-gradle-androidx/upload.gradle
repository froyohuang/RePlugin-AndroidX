apply plugin: 'maven'

uploadArchives {
    repositories {
        mavenDeployer {
            // 本地仓库路径，以放到项目根目录下的 repo 的文件夹为例
            repository(url: uri('../repo'))
        }
    }
}

group = 'com.froyo.replugin'
version = '1.0.0'